(require 'image)
(require 'image-mode)

(setq image-auto-resize 'fit-height)

;; (define-key image-map (kbd "<right>") #'image-next-file)
;; (define-key image-map (kbd "<left>") #'image-previous-file)

(provide 'leaf-image)
